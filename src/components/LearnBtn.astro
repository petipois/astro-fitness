---
interface Props {
  buttonText: string;
}
const { buttonText } = Astro.props;
---

<button
  class="pink tracking-widest text-xl text-white font-bold rounded-full uppercase p-4 w-80"
  >{buttonText}</button
>
